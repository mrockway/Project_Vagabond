<div class="container">
	<div class="row">
		<h1><%= @post.title %></h1>
		<p>Author: 
			<%= @post.user.first_name %></p>
		<p><%= image_tag @post.image.url(:medium) %>
		<p><%= @post.content %></p>
	</div>	
</div>
